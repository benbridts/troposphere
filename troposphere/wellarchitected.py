# Copyright (c) 2012-2026, Mark Peek <mark@peek.org>
# All rights reserved.
#
# See LICENSE file for full license.
#
# *** Do not modify - this file is autogenerated ***


from . import AWSObject, AWSProperty, PropsDictType, Tags


class TagsItems(AWSProperty):
    """
    `TagsItems <http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-properties-wellarchitected-workload-tagsitems.html>`__
    """

    props: PropsDictType = {
        "Key": (str, True),
        "Value": (str, True),
    }


class Lens(AWSObject):
    """
    `Lens <http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-wellarchitected-lens.html>`__
    """

    resource_type = "AWS::WellArchitected::Lens"

    props: PropsDictType = {
        "JSONString": (str, False),
        "LensVersion": (str, False),
        "Tags": ([TagsItems], False),
    }


class ProfileQuestionUpdate(AWSProperty):
    """
    `ProfileQuestionUpdate <http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-properties-wellarchitected-profile-profilequestionupdate.html>`__
    """

    props: PropsDictType = {
        "QuestionId": (str, False),
        "SelectedChoiceIds": ([str], False),
    }


class Profile(AWSObject):
    """
    `Profile <http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-wellarchitected-profile.html>`__
    """

    resource_type = "AWS::WellArchitected::Profile"

    props: PropsDictType = {
        "ProfileDescription": (str, True),
        "ProfileName": (str, True),
        "ProfileQuestions": ([ProfileQuestionUpdate], True),
        "Tags": (Tags, False),
    }


class ReviewTemplate(AWSObject):
    """
    `ReviewTemplate <http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-wellarchitected-reviewtemplate.html>`__
    """

    resource_type = "AWS::WellArchitected::ReviewTemplate"

    props: PropsDictType = {
        "Description": (str, True),
        "Lenses": ([str], True),
        "Notes": (str, False),
        "Tags": ([TagsItems], False),
        "TemplateName": (str, True),
    }


class DiscoveryConfig(AWSProperty):
    """
    `DiscoveryConfig <http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-properties-wellarchitected-workload-discoveryconfig.html>`__
    """

    props: PropsDictType = {
        "TrustedAdvisorIntegrationStatus": (str, False),
        "WorkloadResourceDefinition": ([str], False),
    }


class Workload(AWSObject):
    """
    `Workload <http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-wellarchitected-workload.html>`__
    """

    resource_type = "AWS::WellArchitected::Workload"

    props: PropsDictType = {
        "AccountIds": ([str], False),
        "ArchitecturalDesign": (str, False),
        "AwsRegions": ([str], False),
        "Description": (str, True),
        "DiscoveryConfig": (DiscoveryConfig, False),
        "Environment": (str, True),
        "Industry": (str, False),
        "IndustryType": (str, False),
        "Lenses": ([str], True),
        "NonAwsRegions": ([str], False),
        "Notes": (str, False),
        "ReviewOwner": (str, False),
        "Tags": ([TagsItems], False),
        "WorkloadName": (str, True),
    }
